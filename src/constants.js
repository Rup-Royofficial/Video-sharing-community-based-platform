export const DB_NAME = "video-content-platform";
/* 

After connecting the db using mongoose inside async/await, it is necessary to add the name of the db after the slash 
        await mongoose.connect(`${process.env.MONGODB_URI}/${DB_NAME}`)

*/
